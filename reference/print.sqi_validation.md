# Print method for sqi_validation objects

Surfaces the distribution across decision categories first, because that
is the diagnostic most likely to change a conclusion and the one most
likely to be ignored if buried.

## Usage

``` r
# S3 method for class 'sqi_validation'
print(x, ...)
```

## Arguments

- x:

  An `sqi_validation` object

- ...:

  Additional arguments (not used)

## Value

Invisibly returns the input object
